.env
.env.local
.env.*.local

# Volúmenes / data persistente local
postgres_data/
keycloak_data/

# Realm resuelto desde el template (realm-cartones.json.example) — contiene
# secret del client y dominios reales del entorno, no commitear.
keycloak/realm-cartones.json

# IDE / OS
.idea/
.vscode/
.DS_Store
